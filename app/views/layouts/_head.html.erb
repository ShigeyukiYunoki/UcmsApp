<%= csrf_meta_tags %>
<%= csp_meta_tag %>
<%= include_gon(:init => true) %>

<!-- // メールで服薬通知ができるようになったため、不要だがなぜかOneSignalのエラーがでて、
  // かつスマホだけjqueryが反応しなくなったため、入れておく。OneSignalで通知は切ってある -->
  <script src="https://cdn.onesignal.com/sdks/OneSignalSDK.js" async=""></script>
  <!-- ucms_app_railsコンテナ内でassets:precompileをすると下記がなくても大丈夫に！ -->
  <!-- プッシュ通知復活させて毎日21時にプッシュ通知にする -->
  <script>
    // self.addEventListener("turbolinks:load", () => {
      window.OneSignal = window.OneSignal || [];
      OneSignal.push(function() {
        OneSignal.init({
          appId: "20dfde0b-7c73-490a-b362-46b2a3ecc703",
        });
      });
    // });

  </script>

<meta name="viewport" content="width=device-width, initial-scale=1">
<%# 下記により'javascript/packs/application.js'から作られたstylesheetsファイルパスetc'posts'などをwebpackerから取得できる %>
  <%# media属性は指定なしの場合,'all'が指定されるので省略可 %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
<%# EC2の本番環境では下記がないとjqueryが読み込まれないし、JavaScriptより先に読み込まないといけない %>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <%# 下記により'javascript/packs/application.js'から作られたjavascriptファイルパスetc'post/today'などをwebpackerから取得できる %>
      <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
