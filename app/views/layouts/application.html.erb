<!DOCTYPE html>
<html>
  <head>
    <title><%=full_title(yield(:title))%></title>

    <%= render 'layouts/head' %>

    <%= render 'layouts/shim' %>
  </head>

  <body>

    <%= render 'layouts/header' %>

    <div class="container-fluid pt-5">

      <%= render 'layouts/flash' %>

      <%= yield %>

      <%= debug(params) if Rails.env.development? %>
      <%# debuggerを挙動を確かめたい箇所に挿入し、そのページにアクセスするとdebugが起動する
          終了したい場合はCtl+Dで可能 %>
      </div>

    </body>
  </html>
